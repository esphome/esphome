# Partition table for Matter on ESP32 classic (4MB flash target).
#
# Matter needs a larger NVS than the ESPHome default and a dedicated `fctry`
# namespace for chip-factory (setup_code, discriminator, DAC, etc.). The
# encrypted partitions from the espressif/esp-matter reference (esp_secure_cert,
# nvs_keys) are intentionally omitted here — they require flash-encryption
# fuses that Fase 0 does not exercise. Reintroduce them once we move toward a
# production/certified build.
#
# Layout (fits within 4MB / 0x400000):
#
#   nvs      0x09000  0x0C000   48KB  — Matter chip-config + ESPHome preferences
#   otadata  0x15000  0x02000    8KB
#   phy_init 0x17000  0x01000    4KB
#   ota_0    0x20000  0x1E0000  1.9MB app slot A
#   ota_1    0x200000 0x1E0000  1.9MB app slot B
#   fctry    0x3E0000 0x06000   24KB  — factory data (chip-factory namespace)
#
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0xC000,
otadata,    data, ota,     0x15000,  0x2000,
phy_init,   data, phy,     0x17000,  0x1000,
ota_0,      app,  ota_0,   0x20000,  0x1E0000,
ota_1,      app,  ota_1,   0x200000, 0x1E0000,
fctry,      data, nvs,     0x3E0000, 0x6000,
