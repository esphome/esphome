substitutions:
# Change the disp_name to something you want  
  disp_name: PM12CA
# Interval of how often the power is updated
  update_time: 10s
# frequency of local supply 50 or 60Hz
  frequency: 50Hz
#SCT-013-000 100A/50ma - 32498
  #gain_ct100A: '26077'
  #gain_ct100A: '32498'
  gain_ct100A: '28367'
# SCT-013-050 50A/1V - 15420
  gain_ct50A: '15420'
#SCT-013-030 30A/1V - 9210
  #gain_ct30A: '8650'
  gain_ct30A: '9399'
# Voltage using a 9VAC power transformer
# Note: If you can not get the voltage correct with the highest gain of 65535, uncomment the 2.0 multiply filter above and lower the gain as needed
  gain_v: '34186'
  mult_v: '4.0'
  gain_pga: '1X'

esphome:
  name: power_monitor2
  platform: ESP32
  board: nodemcu-32s

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_pass

# Enable logging
logger:

# Enable Home Assistant API
api:
  password: !secret api_pass

ota:
  password: !secret api_pass

mqtt:
  broker: !secret mqtt_host
  username: !secret mqtt_user
  password: !secret mqtt_pass

web_server:
  port: 80

spi:
  clk_pin: 18
  miso_pin: 19
  mosi_pin: 23
    
switch:
  - platform: restart
    name: ${disp_name} Restart  
time:
  - platform: sntp
    id: sntp_time    
    timezone: !secret timezone


sensor:
  - platform: wifi_signal
    name: ${disp_name} WiFi
    update_interval: 60s

    # channels 1 to 3
  - platform: atm90e32
    cs_pin: 5
    phase_a:
      voltage:
        name: ${disp_name} CT1 Volts
        id: ic1aVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      current:
        name: ${disp_name} CT1 Amps
        id: ct1Amps
      power:
        name: ${disp_name} CT1 Pwr
        id: ct1Pwr
        filters:
          - multiply: ${mult_v}
      gain_ct: ${gain_ct100A}
      reactive_power:
        name: ${disp_name} CT1 Pf
        id: ct1Pf
    phase_b:
      voltage:
        name: ${disp_name} CT2 Volts
        id: ic1bVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      current:
        name: ${disp_name} CT2 Amps
        id: ct2Amps
      power:
        name: ${disp_name} CT2 Pwr
        id: ct2Pwr
        filters:
          - multiply: ${mult_v}
      gain_ct: ${gain_ct100A}
      reactive_power:
        name: ${disp_name} CT2 Pf
        id: ct2Pf
    phase_c:
      voltage:
        name: ${disp_name} CT3 Volts
        id: ic1cVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      current:
        name: ${disp_name} CT3 Amps
        id: ct3Amps
      power:
        name: ${disp_name} CT3 Pwr
        id: ct3Pwr
        filters:
          - multiply: ${mult_v}
      gain_ct: ${gain_ct30A}
      reactive_power:
        name: ${disp_name} CT3 Pf
        id: ct3Pf
    frequency:
      name: ${disp_name} Freq A
    line_frequency: ${frequency}
    gain_pga: ${gain_pga}
    update_interval: ${update_time}
  - platform: template
    name: ${disp_name} CT1 Watts
    id: ct1Watts
    lambda: return id(ct1Amps).state * id(ic1aVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         
  - platform: template
    name: ${disp_name} CT2 Watts
    id: ct2Watts
    lambda: return id(ct2Amps).state * id(ic1bVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         
  - platform: template
    name: ${disp_name} CT3 Watts
    id: ct3Watts
    lambda: return id(ct3Amps).state * id(ic1cVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         

    # channels 4 to 6
  - platform: atm90e32
    cs_pin: 4
    phase_a:
      voltage:
        name: ${disp_name} CT4 Volts
        id: ic2aVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      current:
        name: ${disp_name} CT4 Amps
        id: ct4Amps
      power:
        name: ${disp_name} CT4 Pwr
        id: ct4Pwr
        filters:
          - multiply: ${mult_v}
      gain_ct: ${gain_ct30A}
      reactive_power:
        name: ${disp_name} CT4 Pf
        id: ct4Pf
    phase_b:
      voltage:
        name: ${disp_name} CT5 Volts
        id: ic2bVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      current:
        name: ${disp_name} CT5 Amps
        id: ct5Amps
      power:
        name: ${disp_name} CT5 Pwr
        id: ct5Pwr
        filters:
          - multiply: ${mult_v}
      gain_ct: ${gain_ct30A}
      reactive_power:
        name: ${disp_name} CT5 Pf
        id: ct5Pf
    phase_c:
      voltage:
        name: ${disp_name} CT6 Volts
        id: ic2cVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      current:
        name: ${disp_name} CT6 Amps
        id: ct6Amps
      power:
        name: ${disp_name} CT6 Pwr
        id: ct6Pwr
        filters:
          - multiply: ${mult_v}
      gain_ct: ${gain_ct30A}
      reactive_power:
        name: ${disp_name} CT6 Pf
        id: ct6Pf
    frequency:
      name: ${disp_name} Freq B
    line_frequency: ${frequency}
    gain_pga: ${gain_pga}
    update_interval: ${update_time}    
  - platform: template
    name: ${disp_name} CT4 Watts
    id: ct4Watts
    lambda: return id(ct4Amps).state * id(ic2aVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         
  - platform: template
    name: ${disp_name} CT5 Watts
    id: ct5Watts
    lambda: return id(ct5Amps).state * id(ic2bVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         
  - platform: template
    name: ${disp_name} CT6 Watts
    id: ct6Watts
    lambda: return id(ct6Amps).state * id(ic2cVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         

    # extension board
    # channels 7 to 9
  - platform: atm90e32
    cs_pin: 15
    phase_a:
      voltage:
        name: ${disp_name} CT7 Volts
        id: ic3aVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      current:
        name: ${disp_name} CT7 Amps
        id: ct7Amps
      power:
        name: ${disp_name} CT7 Pwr
        id: ct7Pwr
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      gain_ct: ${gain_ct30A}
      reactive_power:
        name: ${disp_name} CT7 Pf
        id: ct7Pf
    phase_b:
      voltage:
        name: ${disp_name} CT8 Volts
        id: ic3bVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      current:
        name: ${disp_name} CT8 Amps
        id: ct8Amps
      power:
        name: ${disp_name} CT8 Pwr
        id: ct8Pwr
        filters:
          - multiply: ${mult_v}
      gain_ct: ${gain_ct30A}
      reactive_power:
        name: ${disp_name} CT8 Pf
        id: ct8Pf
    phase_c:
      voltage:
        name: ${disp_name} CT9 Volts
        id: ic3cVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      current:
        name: ${disp_name} CT9 Amps
        id: ct9Amps
      power:
        name: ${disp_name} CT9 Pwr
        id: ct9Pwr
        filters:
          - multiply: ${mult_v}
      gain_ct: ${gain_ct30A}
      reactive_power:
        name: ${disp_name} CT9 Pf
        id: ct9Pf
    frequency:
      name: ${disp_name} Freq C
    line_frequency: ${frequency}
    gain_pga: ${gain_pga}
    update_interval: ${update_time}    
  - platform: template
    name: ${disp_name} CT7 Watts
    id: ct7Watts
    lambda: return id(ct7Amps).state * id(ic3aVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         
  - platform: template
    name: ${disp_name} CT8 Watts
    id: ct8Watts
    lambda: return id(ct8Amps).state * id(ic3bVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         
  - platform: template
    name: ${disp_name} CT9 Watts
    id: ct9Watts
    lambda: return id(ct9Amps).state * id(ic3cVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         

    # channels 10 to 12  (or A to C)
  - platform: atm90e32
    cs_pin: 16
    phase_a:
      voltage:
        name: ${disp_name} CT10 Volts
        id: ic4aVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      current:
        name: ${disp_name} CT10 Amps
        id: ct10Amps
      power:
        name: ${disp_name} CT10 Pwr
        id: ct10Pwr
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      gain_ct: ${gain_ct30A}
      reactive_power:
        name: ${disp_name} CT10 Pf
        id: ct10Pf
    phase_b:
      voltage:
        name: ${disp_name} CT11 Volts
        id: ic4bVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      current:
        name: ${disp_name} CT11 Amps
        id: ct11Amps
      power:
        name: ${disp_name} CT11 Pwr
        id: ct11Pwr
        filters:
          - multiply: ${mult_v}
      gain_ct: ${gain_ct30A}
      reactive_power:
        name: ${disp_name} CT11 Pf
        id: ct11Pf
    phase_c:
      voltage:
        name: ${disp_name} CT12 Volts
        id: ic4cVolts
        accuracy_decimals: 1
        filters:
          - multiply: ${mult_v}
      gain_voltage: ${gain_v}
      current:
        name: ${disp_name} CT12 Amps
        id: ct12Amps
      power:
        name: ${disp_name} CT12 Pwr
        id: ct12Pwr
        filters:
          - multiply: ${mult_v}
      gain_ct: ${gain_ct30A}
      reactive_power:
        name: ${disp_name} CT12 Pf
        id: ct12Pf
    frequency:
      name: ${disp_name} Freq D
    line_frequency: ${frequency}
    gain_pga: ${gain_pga}
    update_interval: ${update_time}    
  - platform: template
    name: ${disp_name} CT10 Watts
    id: ct10Watts
    lambda: return id(ct10Amps).state * id(ic4aVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         
  - platform: template
    name: ${disp_name} CT11 Watts
    id: ct11Watts
    lambda: return id(ct11Amps).state * id(ic4bVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         
  - platform: template
    name: ${disp_name} CT12 Watts
    id: ct12Watts
    lambda: return id(ct12Amps).state * id(ic4cVolts).state;
    accuracy_decimals: 0
    unit_of_measurement: VA
    icon: "mdi:flash-circle"
    update_interval: ${update_time}         
