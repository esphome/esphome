esphome:
  name: test-provisioning
  provisioning:
    timeout: 60s
    on_timeout:
      then:
        - lambda: 'return;'

host:

# Provisioning-capable source: encryption enabled, no key.
api:
  encryption:
