# Common UART configuration for ESP8266 Arduino tests - 19200 baud

substitutions:
  tx_pin: GPIO1
  rx_pin: GPIO3

uart:
  - id: uart_bus
    tx_pin: ${tx_pin}
    rx_pin: ${rx_pin}
    baud_rate: 19200
