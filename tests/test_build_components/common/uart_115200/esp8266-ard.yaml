# Common UART configuration for ESP8266 Arduino tests - 115200 baud

substitutions:
  tx_pin: GPIO4
  rx_pin: GPIO5

uart:
  - id: uart_bus
    tx_pin: ${tx_pin}
    rx_pin: ${rx_pin}
    baud_rate: 115200
