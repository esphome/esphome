# Common UART configuration for ESP32-C3 Arduino tests - 1200 baud, EVEN parity, 7 data bits

substitutions:
  tx_pin: GPIO20
  rx_pin: GPIO21

uart:
  - id: uart_bus
    tx_pin: ${tx_pin}
    rx_pin: ${rx_pin}
    baud_rate: 1200
    parity: EVEN
    data_bits: 7
    stop_bits: 1
