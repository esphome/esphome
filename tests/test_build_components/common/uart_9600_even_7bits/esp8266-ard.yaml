# Common UART configuration for ESP8266 Arduino tests - 9600 baud even parity, 7 data bits

substitutions:
  tx_pin: GPIO4
  rx_pin: GPIO5

uart:
  - id: uart_bus
    tx_pin: ${tx_pin}
    rx_pin: ${rx_pin}
    baud_rate: 9600
    parity: EVEN
    data_bits: 7
    stop_bits: 1
