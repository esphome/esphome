# Common UART configuration for ESP32-C3 Arduino tests - 9600 baud even parity

substitutions:
  tx_pin: GPIO20
  rx_pin: GPIO21

uart:
  - id: uart_bus
    tx_pin: ${tx_pin}
    rx_pin: ${rx_pin}
    baud_rate: 9600
    parity: EVEN
