# esp32c6's SPI is free-mux via the GPIO matrix -- any valid pin works.
substitutions:
  clk_pin: "19"
  mosi_pin: "18"
  miso_pin: "20"

spi:
  - id: spi_bus
    clk_pin: ${clk_pin}
    mosi_pin: ${mosi_pin}
    miso_pin: ${miso_pin}
