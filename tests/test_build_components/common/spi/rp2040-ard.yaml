# Common SPI configuration for RP2040 Arduino tests

substitutions:
  clk_pin: GPIO18
  mosi_pin: GPIO19
  miso_pin: GPIO16

spi:
  - id: spi_bus
    clk_pin: ${clk_pin}
    mosi_pin: ${mosi_pin}
    miso_pin: ${miso_pin}
