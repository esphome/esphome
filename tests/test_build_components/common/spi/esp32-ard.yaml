# Common SPI configuration for ESP32 Arduino tests

substitutions:
  clk_pin: GPIO18
  mosi_pin: GPIO23
  miso_pin: GPIO19

spi:
  - id: spi_bus
    clk_pin: ${clk_pin}
    mosi_pin: ${mosi_pin}
    miso_pin: ${miso_pin}
