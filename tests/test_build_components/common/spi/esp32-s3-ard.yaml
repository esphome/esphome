# Common SPI configuration for ESP32-S3 Arduino tests

substitutions:
  clk_pin: GPIO12
  mosi_pin: GPIO11
  miso_pin: GPIO13

spi:
  - id: spi_bus
    clk_pin: ${clk_pin}
    mosi_pin: ${mosi_pin}
    miso_pin: ${miso_pin}
