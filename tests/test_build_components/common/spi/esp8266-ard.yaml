# Common SPI configuration for ESP8266 Arduino tests

substitutions:
  clk_pin: GPIO14
  mosi_pin: GPIO13
  miso_pin: GPIO12

spi:
  - id: spi_bus
    clk_pin: ${clk_pin}
    mosi_pin: ${mosi_pin}
    miso_pin: ${miso_pin}
