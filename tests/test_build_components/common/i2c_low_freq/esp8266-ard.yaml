# Common I2C configuration for ESP8266 Arduino tests - Low Frequency (10kHz)

substitutions:
  scl_pin: GPIO5
  sda_pin: GPIO4

i2c:
  - id: i2c_bus
    scl: ${scl_pin}
    sda: ${sda_pin}
    frequency: 10kHz
    scan: true
