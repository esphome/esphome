# Common I2C configuration for ESP32 Arduino tests - Low Frequency (10kHz)

substitutions:
  scl_pin: GPIO22
  sda_pin: GPIO21

i2c:
  - id: i2c_bus
    scl: ${scl_pin}
    sda: ${sda_pin}
    frequency: 10kHz
    scan: true
