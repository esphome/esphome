# Common UART configuration for ESP8266 Arduino tests - 1200 baud even parity

substitutions:
  tx_pin: GPIO4
  rx_pin: GPIO5

uart:
  - id: uart_bus
    tx_pin: ${tx_pin}
    rx_pin: ${rx_pin}
    baud_rate: 1200
    parity: EVEN
