---
esphome:
  name: test

esp8266:
  board: d1_mini_lite

sensor:
  - platform: adc
    pin: A0
    id: s_1
    name: test s1
    sampling_mode: min
    update_interval: 60s
    device_class: voltage
