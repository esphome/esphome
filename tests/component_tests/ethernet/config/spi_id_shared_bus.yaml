esphome:
  name: test

esp32:
  board: esp32dev

spi:
  - id: spi_bus
    interface: spi2
    clk_pin: GPIO18
    mosi_pin: GPIO23
    miso_pin: GPIO19

ethernet:
  id: eth_component
  type: W5500
  spi_id: spi_bus
  cs_pin: GPIO5
  interrupt_pin: GPIO36
  reset_pin: GPIO22
  clock_speed: 20MHz
