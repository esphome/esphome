esphome:
  name: test

esp32:
  board: esp32dev
  framework:
    type: esp-idf
    advanced:
      disable_mbedtls_tls_server: false
      disable_mbedtls_tls_extras: false

wifi:
  ssid: "test_ssid"
  password: "test_password"

http_request:
  verify_ssl: true
