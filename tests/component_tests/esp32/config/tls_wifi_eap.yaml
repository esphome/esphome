esphome:
  name: test

esp32:
  board: esp32dev
  framework:
    type: esp-idf

wifi:
  networks:
    - ssid: "test_ssid"
      eap:
        username: username
        password: password
        identity: identity
