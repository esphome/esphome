esphome:
  name: test

esp32:
  variant: esp32s3
  framework:
    type: esp-idf

spi:
  clk_pin: GPIO7
  mosi_pin: GPIO9

display:
  - platform: epaper_spi
    id: epaper_display
    model: waveshare-2.9in-v2
    cs_pin: GPIO5
    dc_pin: GPIO17
    reset_pin: GPIO16
    busy_pin: GPIO4
