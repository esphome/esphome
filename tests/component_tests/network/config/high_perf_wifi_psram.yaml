esphome:
  name: test

esp32:
  variant: esp32s3
  framework:
    type: esp-idf

psram:
  mode: octal
  ignore_not_found: false

wifi:
  ssid: "test_ssid"
  password: "test_password"

network:
  enable_high_performance: true
