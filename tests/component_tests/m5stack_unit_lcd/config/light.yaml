esphome:
  name: test

esp32:
  board: esp32dev

i2c:
  sda: GPIO21
  scl: GPIO22

display:
  - platform: m5stack_unit_lcd
    id: unit_lcd
    lambda: |-
      it.draw_pixel_at(0, 0);

light:
  - platform: m5stack_unit_lcd
    id: backlight
    name: Backlight
