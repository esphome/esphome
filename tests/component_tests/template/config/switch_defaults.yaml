---
esphome:
  name: test

esp32:
  board: esp32dev

logger:

switch:
  - platform: template
    id: plain_switch
    turn_on_action:
      - logger.log: "on"
  - platform: template
    id: enabled_switch
    optimistic: true
    assumed_state: true
