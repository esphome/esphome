esphome:
  name: sntp-tz-action-error

esp32:
  board: esp32dev
  framework:
    type: esp-idf

logger:

wifi:
  ssid: "testssid"
  password: "testpassword"

time:
  - platform: sntp
    id: sntp_time

script:
  - id: change_timezone
    then:
      - time.sntp.set_timezone: Australia/Sydney
