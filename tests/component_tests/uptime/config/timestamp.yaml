esphome:
  name: test

esp32:
  board: esp32dev
  framework:
    type: esp-idf

wifi:
  ssid: "test_ssid"
  password: "test_password"

time:
  - platform: sntp
    id: sntp_time

sensor:
  - platform: uptime
    name: Uptime Timestamp
    type: timestamp
