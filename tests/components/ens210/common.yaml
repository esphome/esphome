i2c:
  - id: i2c_ens210
    scl: ${scl_pin}
    sda: ${sda_pin}

sensor:
  - platform: ens210
    temperature:
      name: ENS210 Temperature
    humidity:
      name: ENS210 Humidity
    update_interval: 15s
