esp32:
  board: esp32-c6-devkitc-1
  framework:
    type: esp-idf
    log_level: DEBUG

network:
  enable_ipv6: true

openthread:
  device_type: FTD
  force_dataset: true
  use_address: open-thread-test.local
