spi:
  - id: spi_ens160
    clk_pin: ${clk_pin}
    mosi_pin: ${mosi_pin}
    miso_pin: ${miso_pin}

sensor:
  - platform: ens160_spi
    spi_id: spi_ens160
    cs_pin: ${cs_pin}
    eco2:
      name: "ENS160 eCO2"
    tvoc:
      name: "ENS160 Total Volatile Organic Compounds"
    aqi:
      name: "ENS160 Air Quality Index"

