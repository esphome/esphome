substitutions:
  clk_pin: GPIO5
  mosi_pin: GPIO27
  miso_pin: GPIO4
  cs_pin: GPIO12
  rst_pin: GPIO13
  busy_pin: GPIO25
  dio1_pin: GPIO26

packages:
  spi: !include ../../test_build_components/common/spi/esp32-ard.yaml

<<: !include common.yaml
