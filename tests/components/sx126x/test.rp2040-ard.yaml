substitutions:
  clk_pin: GPIO2
  mosi_pin: GPIO3
  miso_pin: GPIO4
  cs_pin: GPIO5
  rst_pin: GPIO6
  busy_pin: GPIO8
  dio1_pin: GPIO7

packages:
  spi: !include ../../test_build_components/common/spi/rp2040-ard.yaml

<<: !include common.yaml
