i2c:
  - id: i2c_hdc302x
    scl: ${scl_pin}
    sda: ${sda_pin}

sensor:
  - platform: hdc302x
    temperature:
      name: HDC302x Temperature
    humidity:
      name: HDC302x Humidity
    update_interval: 15s
