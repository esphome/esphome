button:
  - platform: template
    name: Send command test
    on_press:
      - emontx.send_command:
          id: test_emontx
          command: "v"

emontx:
  id: test_emontx

sensor:
  - platform: emontx
    name: Power
    tag_name: P1
    emontx_id: test_emontx
    unit_of_measurement: W
