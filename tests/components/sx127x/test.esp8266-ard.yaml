substitutions:
  clk_pin: GPIO0
  mosi_pin: GPIO15
  miso_pin: GPIO16
  cs_pin: GPIO1
  rst_pin: GPIO2
  dio0_pin: GPIO3

packages:
  spi: !include ../../test_build_components/common/spi/esp8266-ard.yaml

<<: !include common.yaml
