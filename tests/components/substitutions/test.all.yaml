esphome:
  name: test

substitutions:
  switch_def:
    platform: template
    name: $name
    optimistic: true
  name: Normal switch
  valid_interpolation: "1"

switch:
  - platform: template
    name: $name
    optimistic: true
  - $switch_def
  - platform: template
    name: Switch ${name} ${valid_interpolation} ${name} ${name}
    optimistic: true

host:
