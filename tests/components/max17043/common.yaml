esphome:
  on_boot:
    then:
      - max17043.sleep_mode: max17043_id

sensor:
  - platform: max17043
    i2c_id: i2c_bus
    id: max17043_id
    battery_voltage:
      name: "Battery Voltage"
    battery_level:
      name: Battery
    update_interval: 10s
