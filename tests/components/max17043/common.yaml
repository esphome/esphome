esphome:
  on_boot:
    then:
      - max17043.sleep_mode: max17043_id

sensor:
  - platform: max17043
    id: max17043_id
    battery_voltage:
      name: "Battery Voltage"
    battery_level:
      name: Battery
    update_interval: 10s
