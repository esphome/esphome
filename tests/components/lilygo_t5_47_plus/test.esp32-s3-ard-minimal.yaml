---
# Minimal test configuration for LILYGO T5 4.7" Plus - no lambda, no network
esphome:
  name: lilygo-t547-test-minimal

packages:
  board: !include ../../../esphome/components/lilygo_t5_47_plus/board_config.yaml

logger:

display:
  - platform: lilygo_t5_47_plus
    update_interval: 60s
