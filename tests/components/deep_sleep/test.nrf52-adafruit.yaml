deep_sleep:
  run_duration: 15s
  sleep_duration: 1h
  wakeup_pin:
    number: P0.07
    mode:
      input: true
      pullup: true
    inverted: true
  wakeup_pin_mode: INVERT_WAKEUP

<<: !include common.yaml

zigbee:

sensor:
  - platform: template
    name: "Temperature"
    id: temperature_sensor
