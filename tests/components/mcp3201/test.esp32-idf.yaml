esphome:
  name: test-mcp3201-esp32
esp32:
  board: esp32dev

substitutions:
  clk_pin: GPIO18
  miso_pin: GPIO19
  cs_pin: GPIO5

packages:
  spi: !include ../../test_build_components/common/spi/esp32-idf.yaml

<<: !include common.yaml
