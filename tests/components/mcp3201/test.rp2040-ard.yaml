esphome:
  name: test-mcp3201-rp2040
rp2040:
  board: rpipicow

substitutions:
  clk_pin: GPIO2
  miso_pin: GPIO4
  cs_pin: GPIO5

packages:
  spi: !include ../../test_build_components/common/spi/rp2040-ard.yaml

<<: !include common.yaml
