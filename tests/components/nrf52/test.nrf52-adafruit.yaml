logger:
  level: VERBOSE

esphome:
  on_boot:
    - lambda: |-
        int x = 100;
        x = constrain(x, 50, 90);
        assert(x == 90);
        x = constrain_at_least(x, 95);
        assert(x == 95);
        x = constrain_at_most(x, 40);
        assert(x == 40);
