spi:
  - id: spi_max6675
    clk_pin: ${clk_pin}
    mosi_pin: ${mosi_pin}
    miso_pin: ${miso_pin}

display:
  - platform: max7219
    cs_pin: ${cs_pin}
    num_chips: 1
    lambda: |-
      it.print("01234567");
