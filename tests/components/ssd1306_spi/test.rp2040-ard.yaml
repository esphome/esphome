substitutions:
  clk_pin: GPIO2
  mosi_pin: GPIO3
  miso_pin: GPIO4
  cs_pin: GPIO5
  dc_pin: GPIO15
  reset_pin: GPIO16
  cs_pin_2: GPIO6
  dc_pin_2: GPIO17
  reset_pin_2: GPIO18

packages:
  spi: !include ../../test_build_components/common/spi/rp2040-ard.yaml

<<: !include common.yaml
