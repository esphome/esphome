# No explicit sda/scl -- exercises the board's own default pinctrl (no overlay
# generated) instead of the packaged bus config used by test.zephyr-esp32-c6.yaml.
i2c:
  - id: i2c_bus
    scan: true

<<: !include common.yaml
