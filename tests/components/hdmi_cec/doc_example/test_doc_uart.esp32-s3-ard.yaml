# test demo example on esp32s3
substitutions:
  name: test_cec_s3
  board: esp32-s3-devkitc-1
  cec_pin: GPIO9
  hpd_pin: GPIO8
  uart_pin: GPIO7
  has_hw_uart: id(cec_uart)->get_hw_serial() != nullptr

<<: !include test_doc_uart_common.yaml
