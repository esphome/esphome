# test demo example on esp32c3
substitutions:
  name: test_cec_c3
  board: seeed_xiao_esp32c3
  framework: esp-idf
  cec_pin: GPIO10
  hpd_pin: GPIO9
  uart_pin: GPIO8
  # GPIO8 and 9 are strapping pins on C3: at least the 'hpd' pull-down seems to NOT work
  has_hw_uart: (!id(cec_uart)->is_failed())

<<: !include test_doc_uart_common.yaml
