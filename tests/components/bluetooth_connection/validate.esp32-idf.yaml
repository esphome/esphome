# The connection classes live in this component since the move out of
# bluetooth_proxy; the proxy's own compile tests cover the builds on every
# platform (CI expands changes here to its dependents), this config pins the
# validation path.
packages:
  common: !include common.yaml

esp32_ble_tracker:

bluetooth_proxy:
  active: true
  connection_slots: 2
