# The esp32 connection class lives in this component since its move out of
# bluetooth_proxy; the proxy's own esp32 compile tests cover the build, this
# config pins the validation path.
packages:
  common: !include common.yaml

esp32_ble_tracker:

bluetooth_proxy:
  active: true
  connection_slots: 2
