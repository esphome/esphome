# The BTstack GATT client backend and hub connection wrapper compile through
# an active bluetooth_proxy, which instantiates them (this component has no
# user-facing configuration of its own).
packages:
  common: !include common.yaml

rp2_ble_tracker:

bluetooth_proxy:
  active: true
  connection_slots: 1
