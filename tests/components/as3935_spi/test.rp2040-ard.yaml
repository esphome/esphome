substitutions:
  clk_pin: GPIO2
  mosi_pin: GPIO3
  miso_pin: GPIO4
  irq_pin: GPIO5
  cs_pin: GPIO6

packages:
  spi: !include ../../test_build_components/common/spi/rp2040-ard.yaml

<<: !include common.yaml
