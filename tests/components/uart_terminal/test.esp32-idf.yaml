substitutions:
  tx_pin: GPIO4
  rx_pin: GPIO5

packages:
  uart: !include ../../test_build_components/common/uart/esp32-idf.yaml

display:
  - platform: uart_terminal
    id: my_lcd
    dimensions: 20x4
    lambda: |-
      it.print("Uart terminal");
