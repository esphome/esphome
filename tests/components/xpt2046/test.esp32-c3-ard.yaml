substitutions:
  clk_pin: GPIO4
  mosi_pin: GPIO5
  miso_pin: GPIO6
  dc_pin: GPIO7
  cs_pin: GPIO0
  disp_cs_pin: GPIO1
  interrupt_pin: GPIO3
  reset_pin: GPIO10

packages:
  spi: !include ../../test_build_components/common/spi/esp32-c3-ard.yaml

<<: !include common.yaml
