substitutions:
  dc_pin: GPIO13
  cs_pin: GPIO14
  disp_cs_pin: GPIO4
  interrupt_pin: GPIO21
  reset_pin: GPIO22

packages:
  spi: !include ../../test_build_components/common/spi/esp32-idf.yaml

<<: !include common.yaml
