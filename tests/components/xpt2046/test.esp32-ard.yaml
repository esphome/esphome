substitutions:
  clk_pin: GPIO17
  mosi_pin: GPIO5
  miso_pin: GPIO12
  dc_pin: GPIO13
  cs_pin: GPIO14
  disp_cs_pin: GPIO4
  interrupt_pin: GPIO15
  reset_pin: GPIO25

packages:
  spi: !include ../../test_build_components/common/spi/esp32-ard.yaml

<<: !include common.yaml
