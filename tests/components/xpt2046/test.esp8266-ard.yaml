substitutions:
  clk_pin: GPIO0
  mosi_pin: GPIO13
  miso_pin: GPIO12
  dc_pin: GPIO15
  cs_pin: GPIO16
  disp_cs_pin: GPIO4
  interrupt_pin: GPIO5
  reset_pin: GPIO2

packages:
  spi: !include ../../test_build_components/common/spi/esp8266-ard.yaml

<<: !include common.yaml
