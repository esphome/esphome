substitutions:
  clk_pin: GPIO2
  mosi_pin: GPIO3
  cs_pin: GPIO5
  dc_pin: GPIO6
  busy_pin: GPIO7
  reset_pin: GPIO8

packages:
  spi: !include ../../test_build_components/common/spi/rp2040-ard.yaml

<<: !include common.yaml
