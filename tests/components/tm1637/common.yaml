display:
  - platform: tm1637
    clk_pin: ${clk_pin}
    dio_pin: ${dio_pin}
    intensity: 3
    lambda: |-
      it.print("1234");
      { const uint8_t buf[] = {0x3F, 0x06, 0x5B, 0x4F, 0x66, 0x6D}; it.set_buffer(buf, sizeof(buf)); }
