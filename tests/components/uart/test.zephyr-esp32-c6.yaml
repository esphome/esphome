esphome:
  on_boot:
    then:
      - uart.write: 'Hello World'
      - uart.write: [0x00, 0x20, 0x42]

uart:
  - id: uart_id
    port: uart0
    baud_rate: 115200
    data_bits: 8
    parity: NONE
    stop_bits: 1
