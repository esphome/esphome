esphome:
  on_boot:
    then:
      - uart.write:
          id: uart_id
          data: 'Hello World'
      - uart.write:
          id: uart_id
          data: [0x00, 0x20, 0x42]

uart:
  # uart0 uses the board's own default pinctrl (no tx_pin/rx_pin), uart1
  # exercises the NRF_PSEL-based remap on a port this board never pinctrl's
  # itself.
  - id: uart_id
    port: uart0
    baud_rate: 115200
    data_bits: 8
    parity: NONE
    stop_bits: 1
  - id: uart_id_pins
    port: uart1
    tx_pin: 22
    rx_pin: 23
    baud_rate: 9600
