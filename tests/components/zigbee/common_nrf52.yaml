packages:
  - !include common.yaml

zigbee:
  model: zigbee_test
  wipe_on_boot: true
  on_join:
    then:
      - logger.log: "Joined network"
  on_start:
    then:
      - logger.log: "Started zigbee stack"

time:
  - platform: zigbee
