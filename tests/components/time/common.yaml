wifi:
  ssid: MySSID
  password: password1

api:

time:
  - platform: homeassistant
    on_time:
      - cron: "*/10 * * * * *"
        then:
          - logger.log: "CronTrigger fired (every 10 seconds)"
  - platform: sntp
    id: sntp_time
