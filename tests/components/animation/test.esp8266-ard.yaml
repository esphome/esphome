packages:
  spi: !include ../../test_build_components/common/spi/esp8266-ard.yaml
  animation: !include common.yaml

display:
  - platform: ili9xxx
    id: main_lcd
    spi_id: spi_bus
    model: ili9342
    cs_pin: 5
    dc_pin: 15
    reset_pin: 16
    invert_colors: false
