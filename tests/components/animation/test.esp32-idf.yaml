packages:
  spi: !include ../../test_build_components/common/spi/esp32-idf.yaml
  animation: !include common.yaml

display:
  - platform: ili9xxx
    id: main_lcd
    spi_id: spi_bus
    model: ili9342
    cs_pin: 12
    dc_pin: 13
    reset_pin: 21
    invert_colors: false
