packages:
  spi: !include ../../test_build_components/common/spi/esp32-c3-idf.yaml
  animation: !include common.yaml

display:
  - platform: ili9xxx
    id: main_lcd
    spi_id: spi_bus
    model: ili9342
    cs_pin: 8
    dc_pin: 9
    reset_pin: 10
    invert_colors: false
