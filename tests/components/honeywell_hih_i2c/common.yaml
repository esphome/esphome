i2c:
  - id: i2c_honeywell_hih
    scl: ${scl_pin}
    sda: ${sda_pin}

sensor:
  - platform: honeywell_hih_i2c
    temperature:
      name: Temperature
    humidity:
      name: Humidity
    update_interval: 15s
