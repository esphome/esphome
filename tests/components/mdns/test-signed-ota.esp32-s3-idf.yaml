# Signed OTA plus the native api: the only combination that emits ota_signed.
esp32:
  variant: esp32s3
  framework:
    type: esp-idf
    advanced:
      signed_ota_verification:

wifi:
  ssid: MySSID
  password: password1

api:

mdns:
