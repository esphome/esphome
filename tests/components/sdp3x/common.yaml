i2c:
  - id: i2c_sdp3x
    scl: ${scl_pin}
    sda: ${sda_pin}

sensor:
  - platform: sdp3x
    id: filter_pressure
    name: HVAC Filter Pressure drop
    accuracy_decimals: 3
    update_interval: 5s
