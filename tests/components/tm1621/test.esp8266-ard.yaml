display:
  - platform: tm1621
    id: tm1621_display
    cs_pin: 15
    data_pin: 14
    read_pin: 12
    write_pin: 13
    lambda: |-
      it.printf(0, "%.1f", 20.0);
      it.display_celsius(true);
      it.printf(1, "%.1f", 20.0);
      it.display_humidity(true);
