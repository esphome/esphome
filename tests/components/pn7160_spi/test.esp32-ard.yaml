substitutions:
  clk_pin: GPIO16
  mosi_pin: GPIO17
  miso_pin: GPIO4
  cs_pin: GPIO12
  irq_pin: GPIO13
  ven_pin: GPIO14

packages:
  spi: !include ../../test_build_components/common/spi/esp32-ard.yaml

<<: !include common.yaml
