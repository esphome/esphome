uart:
  - id: uart_sonoff_d1
    tx_pin: ${tx_pin}
    rx_pin: ${rx_pin}
    baud_rate: 9600

light:
  - platform: sonoff_d1
    id: d1_light
    name: Sonoff D1 Dimmer
    restore_mode: RESTORE_DEFAULT_OFF
    use_rm433_remote: false
