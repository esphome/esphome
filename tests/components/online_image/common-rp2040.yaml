packages:
  spi: !include ../../test_build_components/common/spi/rp2040-ard.yaml

<<: !include common.yaml

display:
  - platform: ili9xxx
    spi_id: spi_bus
    id: main_lcd
    model: ili9342
    cs_pin: 20
    dc_pin: 17
    reset_pin: 21
    invert_colors: true
    lambda: |-
      it.fill(Color(0, 0, 0));
      it.image(0, 0, id(online_rgba_image));
