packages:
  i2c: !include ../../test_build_components/common/i2c/esp32-ard.yaml

<<: !include common.yaml

text_sensor:
  - platform: wifi_info
    power_save_mode:
      name: "WiFi Power Save Mode"
