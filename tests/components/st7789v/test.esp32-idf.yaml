substitutions:
  cs_pin: GPIO12
  dc_pin: GPIO13
  reset_pin: GPIO14
  backlight_pin: GPIO15

packages:
  spi: !include ../../test_build_components/common/spi/esp32-idf.yaml

<<: !include common.yaml
