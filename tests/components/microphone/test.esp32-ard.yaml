substitutions:
  i2s_bclk_pin: GPIO15
  i2s_lrclk_pin: GPIO16
  i2s_mclk_pin: GPIO17
  i2s_din_pin: GPIO33

<<: !include common.yaml

microphone:
  - platform: i2s_audio
    id: mic_id_adc
    adc_pin: 32
    adc_type: internal
