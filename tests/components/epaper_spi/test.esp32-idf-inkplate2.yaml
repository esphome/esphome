packages:
  spi: !include ../../test_build_components/common/spi/esp32-s3-idf.yaml

display:
  - platform: epaper_spi
    model: inkplate2
    rotation: 90
    update_interval: 10s
    lambda: |-
      it.circle(64, 64, 50, Color::BLACK);
