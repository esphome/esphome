# 5000us/2500us sit exactly on the 2.5 ms controller floor: one BLE unit
# apart after truncation, exercising every branch of validate_scan_parameters.
ln882h_ble_tracker:
  id: ble_tracker
  scan_parameters:
    interval: 5000us
    window: 2500us
    duration: 5min
    continuous: false
