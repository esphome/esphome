substitutions:
  light_platform: ledc
  pin_o1: GPIO6
  pin_o2: GPIO7

output:
  - platform: ${light_platform}
    id: light_output_1
    pin: ${pin_o1}
    channel: 0
  - platform: ${light_platform}
    id: light_output_2
    pin: ${pin_o2}
    channel: 1
    phase_angle: 180°

<<: !include common.yaml
