substitutions:
  pin: GPIO2

remote_transmitter:
  - id: xmitr
    pin: ${pin}
    carrier_duty_percent: 50%
    non_blocking: true
    queue_depth: 1

packages:
  buttons: !include common-buttons.yaml
