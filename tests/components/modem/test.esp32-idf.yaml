logger:

modem:
  id: atmodem
  rx_pin: GPIO26
  tx_pin: GPIO27
  status_pin: GPIO34
  power_pin:
    number: GPIO04
    inverted: True
  model: SIM7600
  apn: orange
  pin_code: "0000"
  enable_on_boot: True
  enable_cmux: False
  init_at:
    - AT
  on_not_responding:
    logger.log: "modem not responding"
  on_connect:
    logger.log: "got IP"
  on_disconnect:
    logger.log: "lost IP"

