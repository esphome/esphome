spi:
  - id: spi_adc128s102
    clk_pin: ${clk_pin}
    mosi_pin: ${mosi_pin}
    miso_pin: ${miso_pin}

adc128s102:
  cs_pin: ${cs_pin}
  id: adc128s102_adc

sensor:
  - platform: adc128s102
    id: adc128s102_channel_0
    channel: 0
