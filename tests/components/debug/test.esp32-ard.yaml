<<: !include common.yaml

wifi:
  ssid: "WIFI SSID"
  password: "WIFI PASSWORD"

text_sensor:
  - platform: debug
    power_save_mode:
      name: "WiFi Power Save Mode"

esp32:
  cpu_frequency: 240MHz
