substitutions:
  client_tx_pin: GPIO2
  client_rx_pin: GPIO3
  server_tx_pin: GPIO4
  server_rx_pin: GPIO5
  flow_control_pin: GPIO6

packages:
  i2c: !include ../../test_build_components/common/i2c/rp2040-ard.yaml

<<: !include common.yaml
