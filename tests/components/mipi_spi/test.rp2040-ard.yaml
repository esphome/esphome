substitutions:
  dc_pin: GPIO14
  cs_pin: GPIO13
  clk_pin: GPIO5
  enable_pin: GPIO4
  reset_pin: GPIO20

packages:
  spi: !include ../../test_build_components/common/spi/rp2040-ard.yaml
  display: !include common.yaml
