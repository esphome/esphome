substitutions:
  clk_pin: GPIO16
  mosi_pin: GPIO17
  miso_pin: GPIO15
  dc_pin: GPIO14
  cs_pin: GPIO13
  enable_pin: GPIO19
  reset_pin: GPIO20

display:
  - platform: mipi_spi
    model: LANBON-L8

packages:
  display: !include common.yaml
