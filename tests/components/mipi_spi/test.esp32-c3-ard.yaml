substitutions:
  clk_pin: GPIO6
  mosi_pin: GPIO7
  miso_pin: GPIO5
  dc_pin: GPIO21
  cs_pin: GPIO18
  enable_pin: GPIO19
  reset_pin: GPIO20

packages:
  spi: !include ../../test_build_components/common/spi/esp32-c3-ard.yaml

<<: !include common.yaml
