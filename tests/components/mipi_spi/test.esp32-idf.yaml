substitutions:
  dc_pin: GPIO21
  cs_pin: GPIO12
  enable_pin: GPIO13
  reset_pin: GPIO20

packages:
  spi: !include ../../test_build_components/common/spi/esp32-idf.yaml

<<: !include common.yaml

display:
  - platform: mipi_spi
    model: m5core
