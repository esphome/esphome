substitutions:
  clk_pin: GPIO16
  mosi_pin: GPIO17
  miso_pin: GPIO15
  dc_pin: GPIO21
  cs_pin: GPIO18
  enable_pin: GPIO19
  reset_pin: GPIO20

packages:
  display: !include common.yaml

display:
  - platform: mipi_spi
    model: m5core
