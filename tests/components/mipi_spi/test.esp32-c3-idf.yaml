substitutions:
  dc_pin: GPIO7
  cs_pin: GPIO8
  clk_pin: GPIO5
  enable_pin: GPIO4
  reset_pin: GPIO20

packages:
  spi: !include ../../test_build_components/common/spi/esp32-c3-idf.yaml
  display: !include common.yaml
