i2c:
  - id: i2c_ht16k33_char
    scl: ${scl_pin}
    sda: ${sda_pin}

display:
  - platform: ht16k33_char
    device: ADAFRUIT_14_SEG
    address: 0x70
    max_buffer_length: 12
    lambda: |-
      it.print(0, true, "ABCD");
