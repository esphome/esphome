spi:
  - id: spi_pcd8544
    clk_pin: 6
    mosi_pin: 7
    miso_pin: 5

display:
  - platform: pcd8544
    cs_pin: 2
    dc_pin: 3
    reset_pin: 1
    contrast: 60
    lambda: |-
      it.rectangle(0, 0, it.get_width(), it.get_height());
