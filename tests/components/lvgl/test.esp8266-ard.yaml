spi:
  - id: spi_a
    mosi_pin: GPIO13
    clk_pin: GPIO14

display:
  id: tft_display
  platform: ili9xxx
  model: st7789v
  reset_pin: GPIO2
  dc_pin: GPIO3
  cs_pin: GPIO16
  show_test_card: false
  auto_clear_enabled: false
  update_interval: never

i2c:
  - id: i2c_i2c
    scl: 5
    sda: 4

<<: !include common.yaml

packages:
  lvgl: !include lvgl-package.yaml
