packages:
  i2c: !include ../../test_build_components/common/i2c/esp32-p4-idf.yaml
  esp_video_camera: !include common.yaml

# Frames land in PSRAM; the component refuses to validate without it.
psram:
  mode: hex
  speed: 200MHz
